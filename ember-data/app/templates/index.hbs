
<div class="row">
  <div class="col-sm-12">
    <h2>Projects</h2>
    {{#each model as |project|}}
      <h4>{{project.name}}</h4>
      <h5>Current Campaign</h5>
      {{project.current.name}}
    {{/each}}
  </div>
</div>
